# Database
DATABASE_URL=postgresql://user:password@host:port/database

# Authentication
# Generate with: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10).then(console.log);"
EDITOR_PASSWORD_HASH=$2a$10$examplehashhere

# Site configuration
# REQUIRED: Base URL for the application (used for QR code generation)
# Development: http://localhost:3000
# Production: https://yourdomain.com (no port, use your actual domain or IP)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
